# Make libva find VA-API drivers in both the Mesa GL merge dir (AMD/virtio)
# and the standard DRI path (Intel iHD).
LIBVA_DRIVERS_PATH=/usr/lib/x86_64-linux-gnu/GL/default/lib/dri:/usr/lib/x86_64-linux-gnu/dri
