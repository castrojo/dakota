name: "Feature request"
description: "Propose something new for Bluefin. Community discusses, maintainers approve, then anyone can build it."
labels: ["kind/enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to propose something! 🐟
        Write this in your own words — no AI-generated text for the top sections, please.
        If you opt into **Raptor Current**, `actionadon` will open the community workflow for you. Otherwise this stays a normal issue.

  - type: dropdown
    id: workflow_path
    attributes:
      label: "Workflow path"
      description: "Choose the lighter direct path, or opt into Raptor Current for community discussion → approval → wrangler queue → agent claim."
      options:
        - "Direct issue (skip workflow)"
        - "Raptor Current (community + wrangler + agent queue)"
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: "What problem are you solving?"
      description: "One paragraph. Plain language. What's broken, missing, or annoying today?"
      placeholder: "I keep having to manually push my local build to the zot registry because there's no just recipe for it..."
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: "What do you propose?"
      description: "How should it work? What would done look like?"
      placeholder: "Add a `just push-local` recipe that wraps `podman push` with the lab registry URL pre-filled."
    validations:
      required: true

  - type: textarea
    id: bluefin_fit
    attributes:
      label: "Why does this belong in Bluefin?"
      description: "How does this fit the project's direction? Link to a discussion, prior issue, or upstream if relevant."
    validations:
      required: false

  - type: dropdown
    id: area
    attributes:
      label: "Affected area"
      description: "Pick the closest match — maintainers will refine."
      multiple: true
      options:
        - "area/buildstream"
        - "area/gnome"
        - "area/brew"
        - "area/just"
        - "area/iso"
        - "area/dx"
        - "area/services"
        - "area/flatpak"
        - "area/nvidia"
        - "area/hardware"
        - "area/testing"
        - "area/upstream"
        - "area/policy"
        - "other"
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ### 📋 Acceptance criteria
        *Maintainers fill this in after community discussion. Leave it blank when filing.*

  - type: textarea
    id: acceptance_criteria
    attributes:
      label: "Acceptance criteria"
      description: "Filled in by a maintainer after the issue is approved. Agents use this as their spec."
      placeholder: |
        - [ ] `just push-local` recipe exists and is documented in `just --list`
        - [ ] Recipe pushes to `localhost:5000` by default with `REGISTRY` override
        - [ ] `just validate` passes after the change
      render: markdown
    validations:
      required: false

  - type: textarea
    id: affected_elements
    attributes:
      label: "Affected BST elements (if known)"
      description: "Which elements in elements/bluefin/ or elements/oci/ will change? Leave blank if unsure."
      placeholder: "Justfile only — no BST element changes"
    validations:
      required: false
